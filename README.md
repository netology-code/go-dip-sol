# Blog Management System

Это готовая реализация дипломного проекта на Go для REST API системы управления блогом. Проект демонстрирует корректную реализацию всех требований базового уровня диплома "Go-разработчик с нуля".

## 🎯 О проекте

Реализованное приложение включает:
- ✅ REST API с 8 эндпоинтами
- ✅ Регистрацию и авторизацию пользователей
- ✅ Управление постами (создание и чтение)
- ✅ Управление комментариями (создание и чтение)
- ✅ JWT аутентификацию
- ✅ Хеширование паролей (bcrypt)
- ✅ Отложенное логирование событий через горутину и канал
- ✅ Сохранение данных в JSON файлы
- ✅ Контейнеризацию в Docker

## 📁 Структура проекта

```
reference_impl/
├── cmd/api/
│   └── main.go                 # Точка входа приложения
├── internal/
│   ├── handler/                # HTTP обработчики
│   │   ├── auth_handler.go     # Регистрация и вход
│   │   ├── post_handler.go     # Создание и чтение постов
│   │   ├── comment_handler.go  # Создание и чтение комментариев
│   │   └── utils.go            # Вспомогательные функции
│   ├── middleware/             # HTTP middleware
│   │   ├── auth.go             # JWT аутентификация
│   │   └── logging.go          # Логирование запросов
│   ├── model/                  # Модели данных
│   │   └── models.go           # User, Post, Comment структуры
│   ├── storage/                # Сохранение данных в JSON
│   │   ├── user_storage.go     # Работа с пользователями
│   │   ├── post_storage.go     # Работа с постами
│   │   └── comment_storage.go  # Работа с комментариями
│   ├── logger/                 # Логирование событий
│   │   └── event_logger.go     # Асинхронное логирование
│   └── errors/                 # Ошибки приложения
│       └── apperrors.go        # Переменные ошибок
├── pkg/
│   └── auth/                   # Утилиты аутентификации
│       ├── jwt.go              # JWT токены
│       └── password.go         # Хеширование паролей (bcrypt)
├── data/                       # JSON файлы с данными
│   ├── users.json
│   ├── posts.json
│   └── comments.json
├── logs.txt                    # Логи событий
├── .env.example                # Пример конфигурации
├── docker-compose.yml          # Docker Compose
├── Dockerfile                  # Docker образ
└── go.mod                      # Зависимости проекта
```

## 🚀 Быстрый старт

### Требования

- Go 1.21 или выше
- Docker и Docker Compose (опционально)

### Запуск локально

```bash
# Клонировать проект
git clone <repo-url>
cd reference_impl

# Скопировать конфигурацию
cp .env.example .env

# Скачать зависимости
go mod download

# Запустить приложение
go run cmd/api/main.go
```

Приложение будет доступно на `http://localhost:8080`

### Запуск в Docker

```bash
# Собрать и запустить
docker-compose up --build

# Остановить
docker-compose down
```

## 📡 API эндпоинты

### Публичные эндпоинты

```
GET    /api/health                     # Проверка здоровья API
POST   /api/register                   # Регистрация пользователя
POST   /api/login                      # Вход пользователя
GET    /api/posts                      # Получить все посты
GET    /api/posts/{id}                 # Получить пост по ID
GET    /api/posts/{id}/comments        # Получить комментарии к посту
```

### Защищенные эндпоинты (требуют Authorization: Bearer TOKEN)

```
POST   /api/posts                      # Создать пост
POST   /api/posts/{id}/comments        # Добавить комментарий к посту
```

## 📋 Примеры использования

### Health Check

```bash
curl http://localhost:8080/api/health
```

**Ответ:**
```json
{"status":"ok"}
```

### Регистрация пользователя

```bash
curl -X POST http://localhost:8080/api/register \
  -H "Content-Type: application/json" \
  -d '{
    "username": "john",
    "email": "john@example.com",
    "password": "password123"
  }'
```

**Ответ (201):**
```json
{
  "token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
  "user": {
    "id": 1,
    "username": "john",
    "email": "john@example.com",
    "created_at": "2024-01-15T10:30:00Z"
  }
}
```

### Вход пользователя

```bash
curl -X POST http://localhost:8080/api/login \
  -H "Content-Type: application/json" \
  -d '{
    "email": "john@example.com",
    "password": "password123"
  }'
```

**Ответ (200):**
```json
{
  "token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
  "user": {
    "id": 1,
    "username": "john",
    "email": "john@example.com",
    "created_at": "2024-01-15T10:30:00Z"
  }
}
```

### Создание поста (требуется токен)

```bash
TOKEN="<токен из ответа login>"

curl -X POST http://localhost:8080/api/posts \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer $TOKEN" \
  -d '{
    "title": "My First Post",
    "content": "This is my first blog post"
  }'
```

**Ответ (201):**
```json
{
  "id": 1,
  "title": "My First Post",
  "content": "This is my first blog post",
  "author_id": 1,
  "created_at": "2024-01-15T10:35:00Z"
}
```

### Получение всех постов

```bash
curl http://localhost:8080/api/posts
```

**Ответ (200):**
```json
[
  {
    "id": 1,
    "title": "My First Post",
    "content": "This is my first blog post",
    "author_id": 1,
    "created_at": "2024-01-15T10:35:00Z"
  }
]
```

### Добавление комментария (требуется токен)

```bash
TOKEN="<токен из ответа login>"

curl -X POST http://localhost:8080/api/posts/1/comments \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer $TOKEN" \
  -d '{
    "content": "Great post!"
  }'
```

**Ответ (201):**
```json
{
  "id": 1,
  "content": "Great post!",
  "post_id": 1,
  "author_id": 2,
  "created_at": "2024-01-15T10:40:00Z"
}
```

### Получение комментариев к посту

```bash
curl http://localhost:8080/api/posts/1/comments
```

**Ответ (200):**
```json
[
  {
    "id": 1,
    "content": "Great post!",
    "post_id": 1,
    "author_id": 2,
    "created_at": "2024-01-15T10:40:00Z"
  }
]
```

## ⚙️ Конфигурация

Переменные окружения в файле `.env`:

```env
# Server
SERVER_HOST=0.0.0.0
SERVER_PORT=8080

# JWT
JWT_SECRET=your-secret-key-here
JWT_EXPIRY_HOURS=24

# Data storage
DATA_DIR=./data
LOGS_FILE=./logs.txt

# Environment
ENV=development
```

## 📝 Логирование событий

Приложение логирует создание постов и комментариев в файл `logs.txt`:

```
[2024-01-15 10:35:45] user 1 created post 1
[2024-01-15 10:40:20] user 2 created comment 1
[2024-01-15 10:45:10] user 1 created post 2
```

Логирование реализовано асинхронно:
- **Канал** отправляет события
- **Горутина** записывает их в файл с задержкой
- **Graceful shutdown** корректно завершает логирование при остановке приложения

## 🏗️ Ключевые компоненты

### Storage (JSON файлы)

Приложение сохраняет данные в JSON файлы:

```
data/
├── users.json       # Все пользователи
├── posts.json       # Все посты
└── comments.json    # Все комментарии
```

Синхронизация происходит после каждой операции создания.

### JWT аутентификация

- Токены создаются при регистрации и входе
- Срок действия токена: 24 часа (по умолчанию)
- Проверка происходит в middleware для защищенных эндпоинтов

### Горутины и каналы

Event Logger использует конкурентность Go:

```go
eventsChan := make(chan string, 100)  // Канал для событий
go worker(eventsChan)                 // Горутина-логгер
eventsChan <- "user 1 created post 1" // Отправка события
```

## 🧪 Тестирование

### Unit тесты

```bash
# Запустить все тесты
go test ./...

# С подробным выводом
go test -v ./...

# С проверкой race conditions
go test -race ./...
```

### Интеграционное тестирование

Используйте `test_api.py` для автоматического тестирования API:

```bash
# Требует Python 3
python test_api.py
```

## 📊 Реализованные функции

### Authentication (pkg/auth/)

- `HashPassword(password string) (string, error)` - хеширование паролей
- `CheckPassword(password, hash string) bool` - проверка пароля
- `GenerateToken(userID int, email, username string) (string, time.Time, error)` - создание JWT
- `ValidateToken(tokenString string) (*Claims, error)` - проверка JWT

### Handlers (internal/handler/)

- `AuthHandler.Register()` - POST /api/register
- `AuthHandler.Login()` - POST /api/login
- `PostHandler.Create()` - POST /api/posts
- `PostHandler.GetAll()` - GET /api/posts
- `PostHandler.GetByID()` - GET /api/posts/{id}
- `CommentHandler.Create()` - POST /api/posts/{id}/comments
- `CommentHandler.GetByPost()` - GET /api/posts/{id}/comments

### Middleware (internal/middleware/)

- JWT аутентификация (RequireAuth)
- HTTP логирование (Logger)
- CORS поддержка
- Обработка паник (Recovery)

### Storage (internal/storage/)

- Загрузка и сохранение данных в JSON
- Синхронизация при каждой операции
- Поиск по ID и другим полям
- Проверка уникальности email/username

### Logger (internal/logger/)

- Асинхронное логирование через канал
- Запись событий в файл с timestamp
- Graceful shutdown с завершением горутины
- Задержка 1 секунда для демонстрации асинхронности

## 🔍 Особенности реализации

### Обработка ошибок

Корректная обработка ошибок с правильными HTTP статус кодами:

- `400 Bad Request` - ошибка валидации
- `401 Unauthorized` - неверные учетные данные
- `409 Conflict` - пользователь уже существует
- `404 Not Found` - ресурс не найден
- `500 Internal Server Error` - ошибка сервера

### Валидация данных

- Проверка email формата
- Проверка минимальной длины пароля
- Проверка обязательных полей
- Использование `go-playground/validator`

### Безопасность

- Хеширование паролей с bcrypt
- JWT токены со сроком действия
- Проверка прав доступа (авторизация)
- CORS для контроля доступа

## 📚 Используемые библиотеки

- `github.com/golang-jwt/jwt/v5` - JWT токены
- `golang.org/x/crypto/bcrypt` - хеширование паролей
- `github.com/go-playground/validator/v10` - валидация данных
- `github.com/joho/godotenv` - загрузка .env файлов

## 📦 Версии зависимостей

Смотрите `go.mod` и `go.sum` для полного списка зависимостей.

## 🐳 Docker

Приложение готово к запуску в Docker:

```bash
# Собрать образ
docker build -t blog-api .

# Запустить контейнер
docker run -p 8080:8080 blog-api

# Использовать docker-compose
docker-compose up --build
```

## 📖 Полезные команды

```bash
# Скачать зависимости
go mod download

# Запустить локально
go run cmd/api/main.go

# Собрать бинарь
go build -o api ./cmd/api/main.go
./api

# Запустить тесты
go test ./... -v

# Смотреть логи
tail -f logs.txt

# Просмотреть JSON данные
cat data/users.json | jq

# Запустить в Docker
docker-compose up --build

# Остановить Docker сервисы
docker-compose down
```

## ✅ Чек-лист соответствия требованиям

- ✅ Все 8 эндпоинтов реализованы и работают
- ✅ Регистрация создает уникального пользователя
- ✅ Авторизация выдает JWT токен
- ✅ Посты и комментарии сохраняются в JSON
- ✅ Логирование работает асинхронно (
горутина + канал)
- ✅ Хеширование паролей с bcrypt
- ✅ Правильные HTTP статус коды для всех сценариев
- ✅ Валидация входных данных
- ✅ Middleware для аутентификации и логирования
- ✅ Graceful shutdown с корректным завершением горутин
- ✅ Docker и docker-compose готовы
- ✅ README с примерами использования
- ✅ Код структ
урирован по директориям
- ✅ Все файлы отформатированы (gofmt)

## 🎓 Обучающая ценность

Этот проект демонстрирует:

1. **Основы HTTP** - создание REST API с net/http
2. **Работу с JSON** - маршализация/демаршализация
3. **Структуры и методы** - использование Go структур
4. **Горутины и каналы** - конкурентная обработка
5. **Обработку ошибок** - правильный обработчик ошибок
6. **Аутентификацию** - JWT и хеширование паролей
7. **Структуру проекта** - разделение на слои
8. **Валидацию** - проверку входных данных
9. **Логирование** - асинхронную запись в файл
10. **Docker** - контейнеризацию приложения

## 📞 Поддержка

Если возникли вопросы:

1. Проверьте документацию в README
2. Посмотрите примеры в разделе "Примеры использования"
3. Запустите `go test ./...` для проверки работоспособности
4. Проверьте логи в `logs.txt`

---

**Версия:** 1.0  
**Статус:** Готовая реализация ✅  
**Совместимость:** Go 1.21+
